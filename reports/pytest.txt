1315 passed, 18 skipped, 2 warnings in 497.37s (xdist -n 2; clean, no flakes)

Environment: a fresh checkout with the `learning` extra installed (`uv pip install scikit-learn
joblib`, i.e. `uv sync --all-extras` minus services unavailable here). The 18 skips are optional
dependencies/daemons that self-skip when absent: Docker, OpenHands, the OpenAI SDK (network-gated
here), pgvector, qdrant, and skillopt. A bare `dev`-only checkout (no extras) yields
1297 passed / 22 skipped / 0 failed — the four learned-ML acceptance tests skip via
`importorskip("sklearn")` rather than failing. Reproduce:
    uv run pytest tests/unit tests/integration tests/e2e -n 2 --timeout=300

IMPORTANT: run the suite WITHOUT ACP_ANTHROPIC_API_KEY / ANTHROPIC_API_KEY set. The live agents
are gated on availability; with a real key present, several deterministic workflow tests exercise
the live Claude agent (whose non-deterministic output routes to human review) and fail. The
heterogeneity arena (evals/hetero_arena) is the opposite — it REQUIRES the key, since it is live.
