1363 passed, 15 skipped, 2 warnings in 438.80s (xdist -n 2; clean, no flakes)

Environment: a fresh checkout with the `learning` extra installed (`uv pip install scikit-learn
joblib`, i.e. `uv sync --all-extras` minus services unavailable here). The 15 skips are optional
dependencies/daemons that self-skip when absent: OpenHands, pgvector, qdrant, skillopt, and the
provider-key-gated live embedding/agent paths (Docker is up here, so its checks run). A bare
`dev`-only checkout (no extras) yields 1359 passed / 19 skipped / 0 failed — the four learned-ML
acceptance tests skip via `importorskip("sklearn")` rather than failing. Reproduce:
    uv run pytest tests/unit tests/integration tests/e2e -n 2 --timeout=300

Provider isolation is now enforced automatically: the offline guard in tests/conftest.py strips
provider keys (OpenAI/Anthropic/Gemini/Google) at pytest-configure time unless ACP_TEST_LIVE=1, so
the deterministic suite never reaches a live provider even with keys exported — and ACP_STRICT_OFFLINE=1
fails fast instead of stripping (used by the offline CI job). Live arenas opt in via the
`make test-live-*` targets (which set ACP_TEST_LIVE=1); the heterogeneity arena (evals/hetero_arena)
REQUIRES the key, since it is live.
